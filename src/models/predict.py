"""DDI prediction interface (Module D). Stub until Days 5-6, when the real
GNN is trained on the graph in data/graph/.
"""


def predict(drug_a: str, drug_b: str) -> float:
    """Return the probability that drug_a and drug_b interact.

    STUB: returns a fixed placeholder value. Real implementation loads the
    trained GNN from artifacts/trained_models/ and runs inference.
    """
    return 0.15
