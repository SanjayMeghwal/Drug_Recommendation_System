# Module E — Explainability

**Purpose:** Wrap the trained DDI model (SHAP) to produce a human-readable
explanation for a specific prediction — which factors drove the model's
output.

**Depends on:** Module D (needs a trained model to explain).
**Feeds into:** Module H (Orchestration).
**Type:** Machine Learning / XAI.
