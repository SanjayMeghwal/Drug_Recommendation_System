# tests/

Unit and integration tests for the modules in `src/`. Test files are added
alongside each module as it's implemented, per the milestone plan — every
milestone includes its corresponding tests here, not just the final system.
