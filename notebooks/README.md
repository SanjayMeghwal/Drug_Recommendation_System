# notebooks/

Exploratory work: inspecting raw datasets, prototyping graph construction
logic, sanity-checking model outputs — before that logic is finalized into
`src/`. Not production code; one-off experiments live here, not in `src/`.
