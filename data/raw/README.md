# data/raw/

Untouched data as downloaded from source: DrugBank export and FDA FAERS dump.
Produced by `src/ingestion` (Module A). Never edited by hand or by any later
pipeline stage — this is the fixed starting point we can always reprocess from.

Not committed to git (see `.gitignore`) since DrugBank redistribution is
restricted and FAERS files are large.
