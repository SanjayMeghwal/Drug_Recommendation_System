# data/graph/

The serialized drug-interaction graph (nodes = drugs with feature vectors,
edges = known DDIs, with a train/test edge split) produced by
`src/graph_construction` (Module C) from `data/processed/`.

Not committed to git — regenerate by running the graph construction step.
