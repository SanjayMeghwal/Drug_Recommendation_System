# config/

Central configuration: file paths, model hyperparameters, and the
recommendation module's safety thresholds. Nothing in `src/` should hardcode
these values — they belong here so the full parameter set is visible in one
place (useful when explaining the system in the viva).

See `config.yaml`.
