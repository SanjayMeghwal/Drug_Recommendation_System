# artifacts/trained_models/

Trained model weight files produced by running `src/models` (Module D).
Kept separate from `src/` deliberately: this folder holds generated binary
output, not hand-written source code.

Not committed to git — regenerate by running the training pipeline.
